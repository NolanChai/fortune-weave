---
import CharacterGraphic from './CharacterGraphic.astro';
interface Props { characterId: string; type: 'combat' | 'basic'; }
const { characterId, type } = Astro.props;
---
<CharacterGraphic characterId={characterId} name={type === 'combat' ? 'combatChart' : 'basicChart'} class="stat-chart" />
